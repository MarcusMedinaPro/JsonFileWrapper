# JsonFileWrapper demos

```
dotnet run --project samples/JsonFileWrapper.Demo/JsonFileWrapper.Demo.csproj
```

The sample persists a workspace settings object, prints the structure, and reloads via the implicit conversion operator.
